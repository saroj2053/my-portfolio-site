export interface ResumeEducationSection {
  title: string;
  extra?: string;
  subtitle: string;
  date: string;
  description: string;
  techStack?: string;
}

export interface ResumeEmploymentSection {
  title: string;
  subtitle: string;
  date: string;
  description: string;
}
